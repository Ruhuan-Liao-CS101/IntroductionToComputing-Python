with open( "SYB63_1_202009_Population, Surface Area and Density.csv", "r" ) as infile:
    data = infile.readlines()

    # store the data from line into a list (e.g. ulook into splitting strings)

    # create a class represending each country (i.e. information for each line)

    # create an object per each line and store them into an appropriate data structure (e.g. list, dictionary)

    # implement a search functionlaities (e.g. given a state show 'population density')

    # print( data[10] )
    # print( data[11] )